#include <stdio.h>
#include <stdlib.h>

int main()
{
	int numTimes;
	int place = -1;
	int max = 0;
	int *stack = malloc(sizeof(int*));
	scanf("%d\n", &numTimes);
	for(int i=0; i<numTimes; i++)
	{
		int c, num;
		scanf("%d %d", &case, &num);
		if(c == 1)
		{
		  if(num > max)
				
		}
		if(c == 2)
		{
			
		}
		if(c == 3)
		{

		}
	}
}
