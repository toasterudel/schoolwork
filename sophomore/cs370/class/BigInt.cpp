class BigInt{
    friend ostream& operator<<(ostream& output, const BigInt& p);

    private:
        vector<int> digits;
    public:
        BigInt(int num) {
            
        }
        BigInt &operator *= (const int n){
        
        }
}
